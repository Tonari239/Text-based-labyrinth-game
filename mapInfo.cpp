#include "mapInfo.h"

MapInfo::MapInfo(int level, int monstersCount, int treasuresCount, int rows, int cols)
	:mLevel(level), mMonstersCount(monstersCount), mTreasuresCount(treasuresCount), mRows(rows), mCols(cols)
{

}